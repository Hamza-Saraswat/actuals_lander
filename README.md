# actuals_lander

Landing page for [Actuals](https://github.com/Hamza-Saraswat/actuals) — honest metrics for AI tools.

Single static `index.html`, zero build step, zero external requests, no analytics.

**Deploy on Netlify:** New site → Import from Git → pick this repo → no build command, publish directory `/`. Done.
(Currently also served via GitHub Pages as interim hosting.)
